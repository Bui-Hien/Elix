export const ROLES = {
    ADMIN: "Admin",
    USER: "User",
    SUPER_ADMIN: "SuperAdmin"
} as const;

export const ROUTES = {
    ADMIN_DASHBOARD: "/admin",
    HOME: "/",
    LOGIN: "/login",
} as const;
